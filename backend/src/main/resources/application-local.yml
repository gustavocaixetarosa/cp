server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# JWT Configuration (Local Development)
jwt:
  secret: desenvolvimento_local_secret_key_minimum_32_characters_for_testing
  expiration-ms: 3600000

# Authentication Configuration (Local Development)
auth:
  user:
    email: carol@cpacessoriaecobranca.com
    password: "054058"

# Configuração de Boletos MOCK para desenvolvimento local
bank:
  mock:
    enabled: true  # Ativa estratégia Mock - não requer certificados
  inter:
    api:
      url: https://cdpj.partners.bancointer.com.br
      oauth-url: https://cdpj.partners.bancointer.com.br/oauth/v2/token
    client:
      id: ${INTER_CLIENT_ID:not-needed-in-mock}
      secret: ${INTER_CLIENT_SECRET:not-needed-in-mock}
    certificate:
      path: ${INTER_CERTIFICATE_PATH:}
      password: ${INTER_CERTIFICATE_PASSWORD:}
    scopes: "boleto-cobranca.read boleto-cobranca.write"
