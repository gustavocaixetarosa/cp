# ================================================================
# CPSystem - Environment Variables Template
# ================================================================
# Este arquivo serve como template para configuração de produção.
# NUNCA commite o arquivo .env com valores reais!
#
# Para usar:
# 1. Copie este arquivo: cp env.example .env
# 2. Preencha com valores reais de produção
# 3. Proteja o arquivo: chmod 600 .env
# ================================================================

# ================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# ================================================================
# Configurações do banco de dados PostgreSQL
# IMPORTANTE: Use senhas fortes em produção!

POSTGRES_DB=cpsystem_prod
POSTGRES_USER=cpsystem_user
POSTGRES_PASSWORD=SENHA_FORTE_AQUI_MINIMO_16_CARACTERES

# ================================================================
# BACKEND CONFIGURATION (Spring Boot)
# ================================================================
# Configurações do backend Java/Spring Boot

# URL de conexão JDBC (ajuste o nome do banco se necessário)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/cpsystem_prod

# Credenciais do banco (devem ser iguais às acima)
SPRING_DATASOURCE_USERNAME=cpsystem_user
SPRING_DATASOURCE_PASSWORD=SENHA_FORTE_AQUI_MINIMO_16_CARACTERES

# Perfil Spring (prod para produção)
SPRING_PROFILES_ACTIVE=prod

# Mostrar SQL no console (false para produção)
SPRING_JPA_SHOW_SQL=false

# Opções da JVM (ajuste conforme recursos do servidor)
JAVA_OPTS=-Xmx512m -Xms256m

# ================================================================
# CORS CONFIGURATION
# ================================================================
# Origens permitidas para requisições CORS
# Separe múltiplos domínios com vírgula (sem espaços)
# IMPORTANTE: Configure com seu domínio real em produção!

ALLOWED_ORIGINS=https://seudominio.com,https://www.seudominio.com

# ================================================================
# FRONTEND CONFIGURATION (Next.js)
# ================================================================
# Configurações do frontend React/Next.js

# Ambiente Node.js
NODE_ENV=production

# URL da API (relativo ou absoluto)
# Em produção com Nginx, use caminho relativo ou domínio completo
NEXT_PUBLIC_API_URL=/api/v1
# Ou com domínio: NEXT_PUBLIC_API_URL=https://seudominio.com/api/v1

# ================================================================
# BACKUP CONFIGURATION (AWS S3)
# ================================================================
# Configurações para backup automático no S3

# Nome do bucket S3 para backups
S3_BUCKET=cpsystem-backups-prod

# Região AWS
AWS_REGION=us-east-1

# Dias para manter backups no S3
RETENTION_DAYS=7

# Dias para manter backups locais
LOCAL_RETENTION_DAYS=3

# Nome do container do banco (geralmente não precisa mudar)
DB_CONTAINER=cpsystem-db

# Credenciais do banco para backup (usar mesmas do PostgreSQL)
DB_NAME=cpsystem_prod
DB_USER=cpsystem_user
DB_PASSWORD=SENHA_FORTE_AQUI_MINIMO_16_CARACTERES

# ================================================================
# AWS CREDENTIALS (Opcional)
# ================================================================
# Credenciais AWS para S3 (ou use ~/.aws/credentials)
# Descomente se preferir usar variáveis de ambiente

# AWS_ACCESS_KEY_ID=sua_access_key_aqui
# AWS_SECRET_ACCESS_KEY=sua_secret_key_aqui

# ================================================================
# OPTIONAL SETTINGS
# ================================================================
# Configurações opcionais (já têm valores padrão)

# Diretório para backups temporários
# BACKUP_DIR=/tmp/cpsystem-backups

# Diretório para restore temporário
# RESTORE_DIR=/tmp/cpsystem-restore

# Arquivo de log de backup
# LOG_FILE=/var/log/cpsystem-backup.log

# ================================================================
# NOTAS DE SEGURANÇA
# ================================================================
# 
# 1. NUNCA commite o arquivo .env no Git!
# 2. Use senhas fortes (mínimo 16 caracteres, letras, números e símbolos)
# 3. Senhas de produção devem ser DIFERENTES das de desenvolvimento
# 4. Proteja o arquivo: chmod 600 .env
# 5. Configure CORS apenas com domínios confiáveis
# 6. Use HTTPS em produção
# 7. Considere usar secrets managers (AWS Secrets Manager, Vault, etc.)
#
# ================================================================
